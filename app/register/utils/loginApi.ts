import { NewUser } from "../page";

export const loginUserApi = (newUser: NewUser) => {
  try {
    // Do api request
  } catch (error) {
    //   showToast("Something went wrong. Please try again.");
  }
};
